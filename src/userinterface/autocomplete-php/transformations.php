<?php

/**
 * The following function are intended to transform parts of the hits
 * which are delivered by the complete search server.
 * The must named like "transform_hit_X" where X is the name of the hit entity
 * to be transformed. For example, to transform the %YEAR% entity in the hit template,
 * use "transform_hit_year(&$object, $hit)". 
 *
 * @param unknown_type $object
 * @param unknown_type $hit
 */

?>